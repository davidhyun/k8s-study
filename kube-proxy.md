# kube-proxy

## 역할

**각 워커 노드에서 실행되는 네트워크 프록시**.  
새로운 Service가 생성될 때마다 트래픽을 올바른 Pod로 전달하는 규칙을 노드에 설정한다.

---

## Service가 필요한 이유

```
웹 앱 → DB Pod IP로 직접 접근  ← Pod IP는 재시작 시 변경될 수 있음 (불안정)
웹 앱 → Service(DB) 로 접근    ← 안정적인 고정 엔드포인트 (권장)
```

---

## Service란?

- 실제 컨테이너나 프로세스가 아닌 **Kubernetes 메모리에만 존재하는 가상 컴포넌트**
- IP 주소를 가지지만 Pod 네트워크에 참여하지 않음
- kube-proxy가 이 Service를 실제 Pod로 연결하는 역할을 담당

---

## 동작 방식

```
새 Service 생성
      │
      ▼
kube-proxy 감지
      │
      ▼
각 노드에 iptables 규칙 추가
Service IP(10.96.0.1) → Pod IP(10.32.0.15) 로 트래픽 포워딩
```

---

## 배포 방식

| 배포 방식 | 방법 |
|---|---|
| **kubeadm** | **DaemonSet**으로 배포 → 모든 노드에 Pod 1개씩 자동 배치 |
| **직접 구성** | 바이너리 다운로드 후 서비스로 실행 |
