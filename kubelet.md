# kubelet

## 역할

**워커 노드의 에이전트**. kube-apiserver의 지시를 받아 실제로 Pod를 노드에 실행시킨다.

---

## 주요 책임

1. 노드를 Kubernetes 클러스터에 **등록**
2. Pod/컨테이너 실행 지시를 받아 **컨테이너 런타임(Docker 등)에 이미지 pull 및 실행 요청**
3. Pod와 컨테이너 상태를 **주기적으로 kube-apiserver에 보고**

---

## 다른 컴포넌트와의 차이점

> **kubeadm으로 클러스터를 구성해도 kubelet은 자동 설치되지 않는다.**  
> 워커 노드에 **반드시 수동으로 설치**해야 한다.

---

## 설정 확인 방법

```bash
# 워커 노드에서 실행 중인 프로세스 확인
ps -aux | grep kubelet
```
